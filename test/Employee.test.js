const Employee = require("./lib/Employee");
const inquirer = require('inquirer');
const fs = require('fs');
const chalk = require('chalk');

describe("Employee class", () => {
  describe("getName method", () => {
    it("displays employee name", () => {

        expect(result).toBe(console.log(chalk.green(this.name)));
      });
    });
})
describe("Employee class", () => {
    describe("getID method", () => {
      it("returns employee ID", () => {
  
          expect(result).toBe(console.log(chalk.blue(this.id)));
        });
      });
  })
  describe("Employee class", () => {
    describe("getEmail method", () => {
      it("returns employee email", () => {
  
        expect(result).toBe(console.log(chalk.blue(this.email)));
    });
      });
  })

//   describe("Employee class", () => {
//     describe("getRole method", () => {
//       it("returns employee role", () => {
  
//           expect(result).toBe('');
//         });
//       });
//   })
// The first class is an `Employee` parent class with the following properties and methods:

// * `getName()`

// * `getId()`

// * `getEmail()`

// * `getRole()`&mdash;returns `'Employee'`
